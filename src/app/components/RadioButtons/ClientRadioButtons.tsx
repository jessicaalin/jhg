import RadioButtons from './RadioButtons';

export default function ClientRadioButtons() {
    return (
        <RadioButtons label='Split schedule using social distancing?' optOneValue='yes' optOneLabel='Yes' optTwoValue='no' optTwoLabel='No' />
    )
}